import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Minimal loadable extension used as a template and package smoke check.
 * Replace or remove this file when adding the first real extension.
 */
export default function exampleExtension(_pi: ExtensionAPI): void {
	// Keep the example side-effect free so loading the package is harmless.
}
